# Image-gallery
A responsive Image Gallery built using React that displays images dynamically from an array of objects. The project uses reusable components, props, and the map() method for rendering image cards, with clean component separation and responsive grid-based styling.
